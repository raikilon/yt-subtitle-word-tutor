{
  "name": "yt-subtitle-translate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/bundle-content.js && node scripts/copy-static.js",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4"
  }
}
